import { Button } from 'antd-mobile';

const Home = () => {
    return (
        <div style={{ padding: 24 }}>
            <Button color="primary" fill="solid">
                111111
            </Button>
        </div>
    );
};

export default Home;
